{% trans_default_domain 'FOSUserBundle' %}

<div class="fos_user_user_show mt-5">
    <p>{{ 'profile.show.username'|trans }}: {{ user.username }}</p>
    <p>{{ 'profile.show.email'|trans }}: {{ user.email }}</p>
    <a class="btn btn-outline-primary btn-sm" href="/profile/edit">Edycja profilu</a>
    <a class="btn btn-outline-primary btn-sm" href="/profile/change-password">Zmiana hasła</a>
</div>
