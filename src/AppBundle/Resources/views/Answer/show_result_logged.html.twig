{% extends "default/index.html.twig" %}

{% block javascript %}
    <script src="{{ asset('js/values.js') }}"></script>
    <script src="{{ asset('js/fadeIn.js') }}"></script>
{% endblock %}

{% block content %}
    <table class="table table-sm text-center mainContent">
        <thead>
        <tr>
            <th scope="col">Numer pytania</th>
            <th scope="col">Twoja ocena</th>
            <th scope="col">Zobacz pytanie</th>
        </tr>
        </thead>
        <tbody>
    {% for answer in answers %}
        <tr>
            <td>{{ answer.question.id }}</td>
            <td class="values">{{ answer.value }}</td>
            <td><a class="btn btn-outline-info btn-sm" href="/singleQuestion/{{ answer.question.id }}/">Zobacz</a></td>
        </tr>
    {% endfor %}
        </tbody>
    </table>
{% endblock %}